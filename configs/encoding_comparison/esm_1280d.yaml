seed: 42
save_path: "logs/encoding_comparison/vae_esm_1280d"
data_path: "data/datasets/esm_representation_1280d.npz"

data:
   train_split: 0.8
   normalize: True
   avg_seq_len: True

train:
   num_epochs: 500
   lr: 0.0001
   weight_decay: 0.0001
   batch_size: 128
   warmup_epochs: 100
   kl_start: 0.005
   kl_end: 0.05

model:
   architecture: "vae_esm" # vae_onehot, vae_esm, vae_adj_var
   encoder_dim: [256, 256, 128]
   decoder_dim: [128, 256, 256]
   latent_dim: 2
   gamma_init: null
   eta: null
   a_factor: null
   inducing_points: null
# Results in 1_184_260 params

